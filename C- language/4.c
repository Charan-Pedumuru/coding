#include<stdio.h>
 void main()
 {
     int a;
     printf("Choose the number of extra baggages\n 1.One baggage\n 2.Two baggages\n");
     scanf("%d",&a);
     if(a==1)
        printf("He will carry a weight of 30 kgs");
     else
         printf("He will carry a weight of 53 kgs");
 }
